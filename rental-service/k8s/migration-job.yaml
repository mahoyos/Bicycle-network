apiVersion: batch/v1
kind: Job
metadata:
  name: rental-service-migration
  namespace: bicycle-network
spec:
  backoffLimit: 3
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: migration
          image: 628786078121.dkr.ecr.us-east-1.amazonaws.com/rental-service:latest
          command: ["./rental-service", "--migrate-only"]
          envFrom:
            - secretRef:
                name: rental-service-secrets
          env:
            - name: RUN_MIGRATIONS
              value: "true"
